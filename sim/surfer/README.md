# Surfer Waveform Flow

See `docs/surfer.md` for the user-facing instructions.

This directory contains:

- `open_wave.sh`: helper that opens a waveform in Surfer
- `tb_trigger.sucl`: preloaded view commands for the trigger bench
- `tb_daq_frontend.sucl`: preloaded view commands for the integrated DAQ bench
